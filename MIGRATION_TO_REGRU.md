# Миграция Backend функций на reg.ru

## 📦 Файлы для переноса

### 1. auth-admin (Аутентификация)
**Файлы:**
- `backend/auth-admin/index.py`
- Зависимости: нет

**Environment Variables:**
```
ADMIN_PASSWORD_HASH=ваш_пароль
```

**URL после деплоя:** Замените в `.env` как `VITE_AUTH_URL`

---

### 2. wiki-items (Управление предметами)
**Файлы:**
- `backend/wiki-items/index.py`
- `backend/wiki-items/requirements.txt` (boto3==1.34.0)

**Environment Variables:**
```
AWS_ACCESS_KEY_ID=ваш_ключ_s3
AWS_SECRET_ACCESS_KEY=ваш_секретный_ключ_s3
```

**Endpoint S3:** `https://bucket.poehali.dev`
**Bucket:** `files`
**Файл данных:** `wiki/items.json`

**URL после деплоя:** Замените в `.env` как `VITE_ITEMS_URL`

---

### 3. guides (Управление гайдами)
**Файлы:**
- `backend/guides/index.py`
- `backend/guides/requirements.txt` (boto3==1.34.144)

**Environment Variables:**
```
AWS_ACCESS_KEY_ID=ваш_ключ_s3
AWS_SECRET_ACCESS_KEY=ваш_секретный_ключ_s3
```

**Endpoint S3:** `https://bucket.poehali.dev`
**Bucket:** `files`
**Файл данных:** `wiki/guides.json`

**URL после деплоя:** Замените в `.env` как `VITE_GUIDES_URL`

---

## 🚀 Пошаговая инструкция

### Шаг 1: Подготовка на reg.ru
1. Зайдите на https://www.reg.ru/cloud/
2. Создайте аккаунт / войдите
3. Найдите раздел "Cloud Functions" или "Serverless"
4. Подготовьте 3 новых функции

### Шаг 2: Деплой функций
Для каждой из 3 функций:

1. **Создайте функцию** с параметрами:
   - Runtime: Python 3.11
   - Handler: `index.handler`
   - Memory: 256 MB (минимум)
   - Timeout: 30 секунд

2. **Загрузите код:**
   - Скопируйте содержимое `index.py`
   - Если есть `requirements.txt`, загрузите его тоже

3. **Настройте переменные окружения** (см. выше для каждой функции)

4. **Сохраните URL функции** после деплоя

### Шаг 3: Обновление проекта

1. Создайте файл `.env` в корне проекта:
```env
VITE_AUTH_URL=https://ваш-auth-url.reg.ru
VITE_ITEMS_URL=https://ваш-items-url.reg.ru
VITE_GUIDES_URL=https://ваш-guides-url.reg.ru
```

2. Добавьте `.env` в `.gitignore` (если еще нет)

3. Пересоберите проект:
```bash
npm run build
# или
bun run build
```

### Шаг 4: Проверка
1. Откройте проект в браузере
2. Проверьте работу админ-панели (вход)
3. Проверьте загрузку предметов и гайдов
4. Проверьте создание/редактирование/удаление

---

## 🔧 Альтернативные варианты хостинга

### Yandex Cloud Functions
- URL: https://cloud.yandex.ru/docs/functions/
- Runtime: Python 3.11
- Аналогичная настройка

### AWS Lambda
- URL: https://aws.amazon.com/lambda/
- Runtime: Python 3.11
- Нужно настроить API Gateway

### Vercel Serverless Functions
- URL: https://vercel.com/docs/functions
- Нужно немного изменить формат функций (Next.js style)

---

## 💡 Советы

1. **S3 хранилище**: Можете использовать то же хранилище от poehali.dev или перенести на свое
2. **Тестирование**: Сначала протестируйте одну функцию, потом остальные
3. **Rollback**: Сохраните старые URL на случай проблем
4. **Мониторинг**: Настройте логи на reg.ru для отслеживания ошибок

---

## 📊 Преимущества переноса

✅ Независимость от poehali.dev  
✅ Контроль над ресурсами  
✅ Возможность масштабирования  
✅ Собственный биллинг  
✅ Кастомная настройка окружения  

---

## ⚠️ Важно

- Все 3 функции должны работать на одном S3 хранилище
- Сохраните AWS ключи в безопасном месте
- Не коммитьте `.env` файл в git
- Проверьте CORS настройки на reg.ru

---

## 🆘 Если что-то не работает

1. Проверьте логи функций на reg.ru
2. Убедитесь что все environment variables установлены
3. Проверьте CORS заголовки (должны быть `Access-Control-Allow-Origin: *`)
4. Проверьте формат event/context (может отличаться от Yandex Cloud)
